import { ToggleStyleDirective } from './toggle-style.directive';

describe('ToggleStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleStyleDirective();
    expect(directive).toBeTruthy();
  });
});
